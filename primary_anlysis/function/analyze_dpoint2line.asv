function orthogonal_vertice = analyze_dpoint2line(point_vertice,line_vertices)
%ANALYZE_DPOINT2LINE Summary of this function goes here
%   Detailed explanation goes here
    lstart = line_vertices(1,:);
    lend = line_vertices(2,:);
    origin = point_vertices;
    start2end = lend-lstart;
    start2point = origin - lstart;
    innerproduct = dot(start2end,start2point);
    lstart + ratstart2end;

end

